class Solution {
public:
    bool isArmstrong(int n) {
int sum=0;
 int d=n;
      while(n>0)
      {
        int last=n%10;
        sum=sum+(last*last*last);
        n=n/10;
      }
      return (sum==d);
    }
};
